{
  "name": "crashcore",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter crashcore-frontend dev",
    "build": "bun run --filter crashcore-frontend build",
    "start": "bun run --filter crashcore-frontend start",
    "lint": "bun run --filter crashcore-frontend lint"
  }
}
